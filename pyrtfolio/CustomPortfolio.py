# TODO: include custom portfolio class which just needs name and country values so to calculate the
# volatility of a portfolio for example, so that the user can have the returns, the weights and the 
# covariance matrix in order to calculate their own optimization

class CustomPortfolio(object):
    def __init__(self):
        self.test = 'test'
